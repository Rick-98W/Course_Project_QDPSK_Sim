# QDPSK 数字化信道硬件在环（HIL）通信系统设计蓝图

注意：为了能够直接发到香橙派就可直接使用，系统全程使用 Python 3.10.12 的标准进行开发。先在 PC 上完成两端的基本功能与套接字并发打流测试，随后再将发送端直接无缝移植到香橙派进行部署。

## 一、 项目定位与设计哲学

本设计彻底摒弃了传统通信原理课程设计中“画分立元件电路板（PCB）难落地”、“使用现成蓝牙/Wi-Fi 模块失去物理层算法灵魂”的两个极端。引入现代软件无线电（SDR）**与**硬件在环（Hardware-in-the-Loop, HIL）的尖端工程思想。

整个系统由发射端（香橙派）扮演“远端无线发射基站与虚拟信道环境”，接收端（PC）扮演“高性能多维信号解调与分析统筹中心”。系统采用“双通道不间断并发传输”设计：香橙派端对同一组信源（多模态图文），在底层同时跑两路调制（QPSK 绝对相移通道 与 QDPSK 差分相移通道），并施加完全等同的信道损伤（加性高斯白噪声 AWGN、载波相位旋转偏移），合并打包后通过 UDP 喷射。

接收端（PC 上位机）通过纯手写的**三大同步环路（载波同步、位同步、群同步）**、**绝对相移直接判决**以及**差分相干解调**，在极高噪声与相位偏移环境下对两路受损信号进行实时洗白与多维对比可视化。

## 二、 系统架构与上下位机深度分工

为兼顾嵌入式轻量化部署与上位机重载分析的科学性，系统采用“端侧调制发射 + 物理管道传输 + 云/边侧重载解调分析”的异构分布式架构：

```
+------------------------------------+          +------------------------------------+
|       香橙派 (Orange Pi) 发射端    |          |             PC 上位机接收端         |
|  +------------------------------+  |          |  +------------------------------+  |
|  |     HTML5 极简聊天舱 UI       |  |          |  |     HTML5 SDR 仪表盘网页     |  |
|  +--------------+---------------+  |          |  +--------------^---------------+  |
|                 | (JS 交互)     |  |          |                 | (WebSocket)   |  |
|  +--------------v---------------+  |  UDP     |  +--------------+---------------+  |
|  |     Python 核心发射流模块     |  | Socket   |  |    Python 接收解调与同步算法    |  |
|  | (双通道并行调制/信道污染)     +--+---------->+ (载波/位/群同步、QPSK/QDPSK解调)|  |
|  +------------------------------+  | (局域网) |  +--------------+---------------+  |
+------------------------------------+          |                 | (MSE / PSNR)  |
                                                |  +--------------v---------------+  |
                                                |  |       多维专业通信图表       |  |
                                                |  |  (眼图、星座图、功率谱、PDF导) |  |
                                                |  +------------------------------+  |
                                                +------------------------------------+
```

### 1. 下位机：香橙派 (Orange Pi) —— 远端无线基站与上帝信道

- **多模态信源读取**：提供极简 HTML 聊天窗口，支持现场输入任意文本或现场选择任意本地图片（如宠物图、烤羊排图），并转化为二进制比特流。

- **双通道并行调制（QPSK & QDPSK 实时并发）**：

  - **QPSK 通道（绝对码映射）**：比特流进行 gray 映射直接调制，并通过根升余弦滤波器（RRC Filter）进行基带成型。

  - **QDPSK 通道（差分码映射）**：比特流先通过差分编码器：

    

    $$y_k = (x_k + y_{k-1}) \bmod 4$$

    再进行格雷码映射调制，并通过相同的 RRC 滤波器进行基带成型。

- **虚拟信道损伤（等同污染）**：将两路成型后的基带信号，通过滑块（Slider）实时施加完全相同的数学损伤：

  - *信噪比（SNR / dB）*：叠加等能量的复高斯白噪声。
  - *相位旋转（Phase Rotation / 度）*：模拟信号在空间多径反射导致的固定载波旋转 $r(t) = s(t) e^{j\theta}$。

- **UDP 动态双通道合并喷射**：将加噪/旋转后的 QPSK 波形数据与 QDPSK 波形数据合并封装进同一个自定义 UDP 帧，发送至 PC 端。

### 2. 上位机：PC 电脑 —— 核心算法统筹与分布式处理中心

- **UDP 信号低延迟捕获**：高频监听 Socket 端口，无漏捕获从局域网飞来的双通路基带波形数据。

- **双通道并行实时解调与三大同步（核心算法）**：

  - **位同步（定时跟踪）**：对两路波形同步运行手写的 **Gardner 算法**，精准卡准两路眼图的最大开启时刻。

  - **载波同步（锁相纠偏）**：提取频偏特征，强行锁死两路波形相位差。

  - **QPSK 解调器**：进行绝对码星座图判定，恢复 QPSK 通道比特流。

  - **QDPSK 解调器**：运行**延迟相干解调**：

    

    $$D_k = r_k \cdot r_{k-1}^*$$

    通过相邻符号共轭相乘消去信道旋转角度 $\theta$，完美解决相位模糊。

- **PSNR 与图像恢复质量量化分析**：计算 MSE（均方误差）及 PSNR（峰值信噪比），对 QPSK 和 QDPSK 恢复出的图片进行实时质量差值分析。

- **多维通信图表同步渲染**：PC 端同时渲染 QPSK 与 QDPSK 的时域波形、眼图、频谱及各自的收端星座图，实现直观对比。

- **一键导出 PDF 实验报告**：调用 Python `reportlab`，自动截取当前的双通道性能对比数据、矢量星座图并合成结课报告。

## 三、 演示逻辑与动态交互设计

本设计移除了差分编码的手动使能开关，通过“双通道全程常开”的科学控制变量法，在答辩现场提供极其震撼的对比演示：

### 1. 双通道全程常开设计

- **无脑发送，专注参数**：香橙派端彻底取消“差分使能按钮”。用户在控制端界面上只需要拖动两个最核心的滑块：**【信噪比 SNR】** 与 **【载波相位旋转 Phase Offset】**。
- **同源同宿同信道**：点击发送后，QPSK 与 QDPSK 数据在同一时间，经过完全一样的噪声和相位旋转摧残，并排展示在接收端。

### 2. 接收端无缝多维对比

PC 接收端不需要做任何手动或自动的算法切换，而是将解调逻辑拆分为两条并行的流水线。在黄金视窗中最顶部，一行并排展示三张大图：



$$\text{【原始发送图】} \quad\text{vs}\quad \text{【QPSK 恢复图】} \quad\text{vs}\quad \text{【QDPSK 恢复图】}$$

- **滑块拖动时的降维打击视效**：
  - 当你在香橙派上拉动 **SNR 滑块**时，两张图会同步出现雪花斑点，体现噪声衰减。
  - 当你拉动 **Phase Offset 滑块**时，PC 端的 **QPSK 星座图瞬间整体倾斜**，画面在转过非四象限角度时**一秒变成花屏乱码**；而旁边的 **QDPSK 恢复图纹丝不动、一尘不染**，星座图即使旋转也通过延迟差分相乘在解调器内部被“强制洗白”。

### 3. 事件触发与高频打流联调（Trigger on Change）

- 当控制端图片或文本改变时，自动触发传输；
- 当控制端的 SNR 或 Phase Offset 滑块被拖动时（利用前端 JS 的 `oninput` 事件），每移动一格，就高频连续触发一次香橙派端算法的重算与 UDP 数据喷射。
- 接收端星座图、眼图及图片会随着你在香橙派上指尖的滑动而**丝滑、动态地跟着起舞、旋转或崩溃**，彻底体现软件无线电的实时交互魅力。

## 四、 接收端：对标并超越 MATLAB 的图形阵列

为压榨 Python 矩阵运算和前端 Canvas 硬件加速性能，下方的扩展分析舱通过 Tab 选项卡提供多达 10 张工业级通信图表：

| **序号** | **图表名称**                       | **观测端**      | **答辩时说辞与硬核展示**                                     |
| -------- | ---------------------------------- | --------------- | ------------------------------------------------------------ |
| **1**    | **发端基带眼图 (Eye Diagram)**     | 下位机 (香橙派) | 展示基带信号经过 RRC 成型后的完美眼图。眼线清晰无 ISI。      |
| **2**    | **发端干净星座图 (Constellation)** | 下位机 (香橙派) | 标准 4 点星座分布图，干净一尘不染。                          |
| **3**    | **发端基带信号功率谱 (PSD)**       | 下位机 (香橙派) | 展示主旁瓣，证明信号带宽满足低通与带通要求。                 |
| **4**    | **收端被污染的时域波形图**         | 上位机 (PC)     | 展示信号传输后，沾满高频杂波毛刺、彻底失真的时域波形。       |
| **5**    | **收端未同步的“旋转”星座图**       | 上位机 (PC)     | 星座图化为疯狂旋转的“圆环”，直观展示绝对映射由于频偏崩溃的原因。 |
| **6**    | **Gardner 位同步误差收敛曲线**     | 上位机 (PC)     | 动态画出定时误差（TED）如何收敛到 0，精确卡准符号正中心。    |
| **7**    | **QPSK 接收判决星座图**            | 上位机 (PC)     | 受到相位旋转污染后，点位整体发生物理倾斜，偏出决策边界。     |
| **8**    | **QDPSK 差分相乘后星座图**         | 上位机 (PC)     | 经历延迟共轭相乘后，旋转偏角自动消去，星座图重新收敛凝聚成 4 个聚集点。 |
| **9**    | **群同步相关系数峰值图**           | 上位机 (PC)     | 在帧头巴克码位置冲出一个锐利的脉冲尖峰，证明精准捕获帧头。   |
| **10**   | **蒙特卡罗误码率对标曲线 (BER)**   | 上位机 (PC)     | 完美对标 MATLAB 的理论公式线，用多组散点展示实测值逼近理论。 |

## 五、 自定义 UDP 网络通信协议

为确保双通道数据合并传输时的边界与解耦，两端必须严格遵守统一的自定义应用层帧格式（信封）：

```
+------------------+------------------+-----------------------+-----------------------+-----------------------------+
|   帧头 (2 Bytes)  |  模式字节 (1 B)   | QPSK 数据长度 (4 B)   | QDPSK 数据长度 (4 B)  |   数据负载 (N + M Bytes)    |
|   0xAA  0xBB     | 0x03 (双路并发)  |  Int32 (Big-Endian)   |  Int32 (Big-Endian)   | [QPSK Payload]+[QDPSK Payload]|
+------------------+------------------+-----------------------+-----------------------+-----------------------------+
```

### 字段详细解析

- **帧头 (Header)**：固定为 `0xAA 0xBB`。用于让接收端在网卡缓冲区中进行字节流检索，识别出一帧数据的开始。
- **模式字节 (Mode)**：固定为 `0x03`。代表当前数据流为 **QPSK & QDPSK 双路波形并发模式**。
- **数据长度 (Length)**：分别声明紧随其后的 QPSK 信号数据长度与 QDPSK 信号数据长度。
- **数据负载 (Payload)**：QPSK 的受损 IQ 波形点阵，以及紧跟其后的 QDPSK 的受损 IQ 波形点阵。

## 六、 工作流与路线图规划

### 【第一阶段：核心算法 NumPy 双通道跑通（链路成型）】

- **任务**：在单机 PC 上，使用 NumPy 实现 `信源 -> (绝对码 / 差分码) -> 映射 -> AWGN & 旋转信道 -> (直接判决 / 差分相乘解调) -> 双图像恢复`，在终端打印两路各自的误码率和 PSNR，确保核心 DSP 算法 100% 正确。

### 【第二阶段：攻克三大同步与双通道 UDP 协议（打通物理管道）】

- **任务**：
  - 在接收机中手写加入巴克码相关器（群同步）与 Gardner 插值算法（位同步）。
  - 编写香橙派与 PC 之间的 UDP 套接字，实现单包内双通道波形数据的封包、网络喷射、拆包与多路分流。

### 【第三阶段：构建 GUI 仪表盘与大屏渲染（打磨面子）】

- **任务**：
  - 用 Flask/FastAPI 与 WebSocket 建立后端，使用 HTML5/Canvas 编写前端监控面板。
  - 实现双通道星座图、眼图、频谱、群同步跳变峰值以及 PSNR 量化看板的实时、流畅显示。
  - 进行局域网热点实车调试，香橙派运行发送脚本，PC 运行接收解调，大屏全屏显示。

### 【第四阶段：大论文撰写与答辩准备（降维打击）】

- **任务**：
  - 截图双路并发下，噪声和相位旋转对 QPSK 与 QDPSK 产生的变量对比图像及 PSNR 分析。
  - 一键导出印刷级 PDF 通信报告，论文末尾写入 “FPGA(高速算法下沉) + 香橙派(SPI硬件数据抽取)” 的未来异构扩展蓝图，全优结课。
# Current Implementation Note - 2026-06-15

The blueprint is still useful as a systems-level vision, but the implemented demo has these current constraints:

- The live source image is fixed to `raw_pic_64.png` and `64x64 RGB`.
- The Transmitter/Receiver demo uses fragmented `complex64` IQ UDP packets with a 26-byte application header.
- The earlier single-packet `AA BB 03 + qpsk_len + qdpsk_len + payload` idea is not the current protocol.
- Receiver fallback image recovery is intentionally fixed to `64x64x3` RGB so degraded frames still produce a comparable PNG.
- Demo SNR and phase rotation are clamped to `6.0 .. 30.0 dB` and `-30 .. +30 deg`.
